// Resize IMAGE  
const uploadZone=document.getElementById('upload-zone'),selectBtn=document.getElementById('select-btn'),fileInput=document.getElementById('file-input'),workArea=document.getElementById('work-area'),loadingOverlay=document.getElementById('loading-overlay'),thumbnailImg=document.getElementById('thumbnail-img'),imageName=document.getElementById('image-name'),controlsContent=document.getElementById('controls-content'),processBtn=document.getElementById('process-btn'),downloadBtn=document.getElementById('download-btn');let currentImage=null,originalFile=null,processedBlob=null;controlsContent.innerHTML=`<h3>Resize Options</h3><div class="control-item"><label class="control-label">Width (px)</label><input type="number" class="input-field" id="width-input" value="800" min="1"></div><div class="control-item"><label class="control-label">Height (px)</label><input type="number" class="input-field" id="height-input" value="600" min="1"></div><div class="control-item"><label class="control-label">Resize by percentage</label><input type="range" class="slider" id="scale-slider" min="10" max="200" value="100"><div class="slider-labels"><span>10%</span><span id="scale-value">100%</span><span>200%</span></div></div>`;processBtn.textContent='Resize IMAGE';selectBtn.addEventListener('click',e=>{e.stopPropagation();fileInput.click()});fileInput.addEventListener('change',e=>{if(e.target.files.length>0)handleFile(e.target.files[0])});uploadZone.addEventListener('dragover',e=>{e.preventDefault();uploadZone.classList.add('dragover')});uploadZone.addEventListener('dragleave',()=>uploadZone.classList.remove('dragover'));uploadZone.addEventListener('drop',e=>{e.preventDefault();uploadZone.classList.remove('dragover');if(e.dataTransfer.files.length>0)handleFile(e.dataTransfer.files[0])});setTimeout(()=>{const scaleSlider=document.getElementById('scale-slider'),scaleValue=document.getElementById('scale-value');if(scaleSlider)scaleSlider.addEventListener('input',e=>{scaleValue.textContent=e.target.value+'%';if(currentImage){const scale=e.target.value/100;document.getElementById('width-input').value=Math.round(currentImage.width*scale);document.getElementById('height-input').value=Math.round(currentImage.height*scale)}})},100);processBtn.addEventListener('click',resizeImage);downloadBtn.addEventListener('click',downloadImage);async function handleFile(file){if(!file.type.startsWith('image/')){alert('Please select an image file');return}originalFile=file;loadingOverlay.style.display='flex';try{const img=await loadImage(file);currentImage=img;const thumbnail=await createThumbnail(img,300);thumbnailImg.src=thumbnail;imageName.textContent=file.name;document.getElementById('width-input').value=img.width;document.getElementById('height-input').value=img.height;uploadZone.style.display='none';workArea.style.display='grid';loadingOverlay.style.display='none';downloadBtn.style.display='none'}catch(error){alert('Error loading image');loadingOverlay.style.display='none'}}async function resizeImage(){const width=parseInt(document.getElementById('width-input').value),height=parseInt(document.getElementById('height-input').value);processBtn.disabled=true;processBtn.textContent='Resizing...';loadingOverlay.style.display='flex';setTimeout(()=>{const canvas=document.createElement('canvas'),ctx=canvas.getContext('2d');canvas.width=width;canvas.height=height;ctx.drawImage(currentImage,0,0,width,height);canvas.toBlob(blob=>{processedBlob=blob;downloadBtn.style.display='block';processBtn.textContent='Resize IMAGE';processBtn.disabled=false;loadingOverlay.style.display='none'},'image/jpeg',0.9)},600)}function downloadImage(){if(processedBlob){const url=URL.createObjectURL(processedBlob),a=document.createElement('a');a.href=url;a.download=`resized-${Date.now()}.jpg`;a.click();URL.revokeObjectURL(url)}}function loadImage(file){return new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=reject;img.src=URL.createObjectURL(file)})}function createThumbnail(img,maxSize){return new Promise(resolve=>{const canvas=document.createElement('canvas'),ctx=canvas.getContext('2d');let width=img.width,height=img.height;if(width>height){if(width>maxSize){height=height*maxSize/width;width=maxSize}}else{if(height>maxSize){width=width*maxSize/height;height=maxSize}}canvas.width=width;canvas.height=height;ctx.drawImage(img,0,0,width,height);resolve(canvas.toDataURL('image/jpeg',0.9))})}