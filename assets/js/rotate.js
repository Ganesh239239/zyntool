// Rotate
const uploadZone=document.getElementById('upload-zone'),selectBtn=document.getElementById('select-btn'),fileInput=document.getElementById('file-input'),workArea=document.getElementById('work-area'),loadingOverlay=document.getElementById('loading-overlay'),thumbnailImg=document.getElementById('thumbnail-img'),imageName=document.getElementById('image-name'),controlsContent=document.getElementById('controls-content'),processBtn=document.getElementById('process-btn'),downloadBtn=document.getElementById('download-btn');let currentImage=null,originalFile=null,processedBlob=null,currentRotation=0;controlsContent.innerHTML=`<h3>Rotate</h3><div class="control-item"><label class="control-label">Quick Rotate</label><div class="button-group"><button class="btn-secondary" id="rotate-90">↻ 90°</button><button class="btn-secondary" id="rotate-180">↻ 180°</button><button class="btn-secondary" id="rotate-270">↻ 270°</button></div></div><div class="info-box"><p>Rotation: <strong><span id="rotation-display">0°</span></strong></p></div>`;processBtn.textContent='Apply';selectBtn.addEventListener('click',e=>{e.stopPropagation();fileInput.click()});fileInput.addEventListener('change',e=>{if(e.target.files.length>0)handleFile(e.target.files[0])});uploadZone.addEventListener('dragover',e=>{e.preventDefault();uploadZone.classList.add('dragover')});uploadZone.addEventListener('dragleave',()=>uploadZone.classList.remove('dragover'));uploadZone.addEventListener('drop',e=>{e.preventDefault();uploadZone.classList.remove('dragover');if(e.dataTransfer.files.length>0)handleFile(e.dataTransfer.files[0])});setTimeout(()=>{document.getElementById('rotate-90').addEventListener('click',()=>{currentRotation=(currentRotation+90)%360;updatePreview()});document.getElementById('rotate-180').addEventListener('click',()=>{currentRotation=(currentRotation+180)%360;updatePreview()});document.getElementById('rotate-270').addEventListener('click',()=>{currentRotation=(currentRotation+270)%360;updatePreview()})},100);processBtn.addEventListener('click',rotateImage);downloadBtn.addEventListener('click',downloadImage);function updatePreview(){document.getElementById('rotation-display').textContent=currentRotation+'°';thumbnailImg.style.transform=`rotate(${currentRotation}deg)`;thumbnailImg.style.transition='transform 0.3s'}async function handleFile(file){if(!file.type.startsWith('image/')){alert('Please select an image file');return}originalFile=file;loadingOverlay.style.display='flex';try{const img=await loadImage(file);currentImage=img;const thumbnail=await createThumbnail(img,300);thumbnailImg.src=thumbnail;imageName.textContent=file.name;currentRotation=0;updatePreview();uploadZone.style.display='none';workArea.style.display='grid';loadingOverlay.style.display='none';downloadBtn.style.display='none'}catch(error){alert('Error loading image');loadingOverlay.style.display='none'}}async function rotateImage(){processBtn.disabled=true;processBtn.textContent='Processing...';loadingOverlay.style.display='flex';setTimeout(()=>{const canvas=document.createElement('canvas'),ctx=canvas.getContext('2d');let w=currentImage.width,h=currentImage.height;if(currentRotation===90||currentRotation===270){canvas.width=h;canvas.height=w}else{canvas.width=w;canvas.height=h}ctx.translate(canvas.width/2,canvas.height/2);ctx.rotate(currentRotation*Math.PI/180);ctx.drawImage(currentImage,-w/2,-h/2,w,h);canvas.toBlob(blob=>{processedBlob=blob;downloadBtn.style.display='block';processBtn.textContent='Apply';processBtn.disabled=false;loadingOverlay.style.display='none'},'image/jpeg',0.9)},600)}function downloadImage(){if(processedBlob){const url=URL.createObjectURL(processedBlob),a=document.createElement('a');a.href=url;a.download=`rotated-${Date.now()}.jpg`;a.click();URL.revokeObjectURL(url)}}function loadImage(file){return new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=reject;img.src=URL.createObjectURL(file)})}function createThumbnail(img,maxSize){return new Promise(resolve=>{const canvas=document.createElement('canvas'),ctx=canvas.getContext('2d');let width=img.width,height=img.height;if(width>height){if(width>maxSize){height=height*maxSize/width;width=maxSize}}else{if(height>maxSize){width=width*maxSize/height;height=maxSize}}canvas.width=width;canvas.height=height;ctx.drawImage(img,0,0,width,height);resolve(canvas.toDataURL('image/jpeg',0.9))})}