---
const { entry, relatedTools } = Astro.props;
---

<div id="compress-tool">
    <!-- STEP 1: UPLOAD BOX (Kept exactly as you like it) -->
    <div id="uploadContainer" class="uploader-card shadow-lg mb-5 bg-white">
        <div class="uploader-border-wrapper p-3"> 
            <div class="uploader-zone text-center" onclick="document.getElementById('fileInput').click()" style="cursor:pointer">
                <div class="icon-circle mb-3 mx-auto" style={`background: ${entry.data.color}15; color: ${entry.data.color}; width:80px; height:80px; border-radius:50%; display:flex; align-items:center; justify-content:center;`}>
                    <i class={`fa-solid ${entry.data.icon} fa-3x`}></i>
                </div>
                <h2 class="fw-bold text-dark">Select Images</h2>
                <p class="text-muted small">Upload multiple images to compress in bulk</p>
                <input type="file" id="fileInput" hidden accept="image/jpeg,image/png,image/webp" multiple />
            </div>
        </div>
    </div>

    <!-- STEP 2: PROFESSIONAL BATCH WORKSPACE -->
    <div id="processingArea" style="display:none;" class="fade-in">
        <!-- IMAGE QUEUE SELECTOR -->
        <div id="fileQueue" class="d-flex gap-3 mb-4 overflow-auto pb-3 custom-scrollbar">
            <!-- Thumbnails injected here -->
        </div>

        <div class="row g-4 text-start">
            <!-- COMPARISON STAGE -->
            <div class="col-lg-8">
                <div class="workspace-stage shadow-sm rounded-4 border bg-white overflow-hidden">
                    <div class="stage-header p-3 border-bottom d-flex justify-content-between align-items-center bg-light">
                        <span class="badge bg-white text-dark border shadow-sm fw-bold">Live Comparison</span>
                        <div id="activeFileName" class="small fw-bold text-muted text-truncate ms-2" style="max-width: 200px;">filename.jpg</div>
                    </div>
                    
                    <div class="row g-0 image-viewport">
                        <!-- Original Side -->
                        <div class="col-6 border-end position-relative transparency-grid">
                            <div class="comp-label">ORIGINAL</div>
                            <img id="viewBefore" src="" class="img-fluid p-3">
                            <div class="size-tag" id="sizeBefore">0 KB</div>
                        </div>
                        <!-- Compressed Side -->
                        <div class="col-6 position-relative transparency-grid">
                            <div class="comp-label">COMPRESSED</div>
                            <img id="viewAfter" src="" class="img-fluid p-3">
                            <div class="size-tag text-success" id="sizeAfter">0 KB</div>
                        </div>
                    </div>

                    <div id="miniLoader" class="loader-overlay d-none">
                        <div class="spinner-border text-primary mb-2"></div>
                        <span class="fw-bold small uppercase">Optimizing...</span>
                    </div>
                </div>
            </div>

            <!-- SIDEBAR CONTROLS -->
            <div class="col-lg-4">
                <div class="sidebar-sticky">
                    <div class="card border-0 shadow-sm rounded-4 p-4 mb-4 bg-white">
                        <div class="text-center p-3 bg-light rounded-4 mb-4">
                            <div class="display-6 fw-black text-primary" id="savingsPct">-0%</div>
                            <p class="extra-small fw-bold text-muted uppercase mt-1 mb-0">Space Saved</p>
                        </div>

                        <div class="mb-4">
                            <label class="small fw-bold text-muted d-flex justify-content-between mb-2">
                                Compression Quality <span id="qVal" class="badge bg-primary">70</span>
                            </label>
                            <input type="range" id="qIn" class="form-range" min="5" max="100" step="1" value="70">
                            <div class="d-flex justify-content-between extra-small text-muted mt-2">
                                <span>Small File</span>
                                <span>High Quality</span>
                            </div>
                        </div>

                        <div class="d-grid gap-2">
                            <button id="btnDownloadActive" class="btn btn-primary rounded-pill py-2 fw-bold shadow-sm">
                                <i class="fa-solid fa-download me-2"></i> Download Current
                            </button>
                            <button id="btnDownloadAll" class="btn btn-dark rounded-pill py-2 fw-bold shadow-sm">
                                <i class="fa-solid fa-file-zipper me-2"></i> Download All (ZIP)
                            </button>
                        </div>
                    </div>

                    <!-- RELATED TOOLS -->
                    <div class="card border-0 shadow-sm rounded-4 p-4 bg-white d-none d-lg-block">
                        <p class="extra-small fw-bold text-muted uppercase mb-3 letter-spacing-1">Related Tools</p>
                        <div class="row g-2">
                            {relatedTools.map(t => (
                                <div class="col-6">
                                    <a href={`/${t.slug || t.id}`} class="text-decoration-none small text-dark d-flex align-items-center p-2 border rounded-3 sidebar-tool-link">
                                        <i class={`fa-solid ${t.data.icon} me-2`} style={`color: ${t.data.color}`}></i>
                                        <span class="text-truncate">{t.data.title.split(' ')[0]}</span>
                                    </a>
                                </div>
                            ))}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .fw-black { font-weight: 900; }
    .extra-small { font-size: 0.65rem; font-weight: 800; letter-spacing: 0.5px; }
    
    /* uploader-card fix from previous step */
    .uploader-card { border-radius: 24px; overflow: visible; }
    .uploader-zone { border: 3px dashed #cbd5e1; border-radius: 20px; padding: 80px 20px; transition: 0.3s; background: #fff; }
    .uploader-zone:hover { border-color: #4B90FF; background: #f8faff; border-style: solid; }

    /* Comparison Elements */
    .image-viewport { min-height: 400px; background: #f1f5f9; }
    .image-viewport img { max-height: 350px; width: 100%; object-fit: contain; }
    .transparency-grid { background-image: radial-gradient(#cbd5e0 1px, transparent 1px); background-size: 15px 15px; }
    
    .comp-label { position: absolute; top: 10px; left: 10px; background: rgba(0,0,0,0.6); color: #fff; font-size: 0.6rem; font-weight: 900; padding: 2px 8px; border-radius: 4px; }
    .size-tag { position: absolute; bottom: 10px; left: 50%; transform: translateX(-50%); background: #fff; padding: 2px 12px; border-radius: 50px; font-size: 0.7rem; font-weight: 800; box-shadow: 0 4px 10px rgba(0,0,0,0.1); }

    /* Thumbnail Queue */
    .q-thumb { width: 70px; height: 70px; border-radius: 12px; border: 2px solid #e2e8f0; cursor: pointer; flex-shrink: 0; position: relative; background: #fff; transition: 0.2s; }
    .q-thumb.active { border-color: #4B90FF; box-shadow: 0 0 0 3px rgba(75,144,255,0.1); }
    .q-thumb img { width: 100%; height: 100%; object-fit: cover; border-radius: 10px; }
    .q-savings { position: absolute; bottom: -5px; right: -5px; background: #4CAF50; color: #fff; font-size: 0.55rem; font-weight: 900; padding: 1px 4px; border-radius: 4px; }

    .sidebar-sticky { position: sticky; top: 100px; }
    .loader-overlay { position: absolute; inset: 0; background: rgba(255,255,255,0.7); display: flex; flex-direction: column; align-items: center; justify-content: center; z-index: 10; }
    .custom-scrollbar::-webkit-scrollbar { height: 6px; }
    .custom-scrollbar::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 10px; }
</style>

<script is:inline>
    const fileInput = document.getElementById('fileInput');
    const uploadContainer = document.getElementById('uploadContainer');
    const processingArea = document.getElementById('processingArea');
    const queueContainer = document.getElementById('fileQueue');
    const viewBefore = document.getElementById('viewBefore');
    const viewAfter = document.getElementById('viewAfter');
    const qIn = document.getElementById('qIn');
    const qVal = document.getElementById('qVal');
    const miniLoader = document.getElementById('miniLoader');

    let queue = [];
    let activeIndex = 0;

    function formatBytes(bytes) {
        if (bytes === 0) return '0 KB';
        const k = 1024;
        const i = Math.floor(Math.log(bytes) / Math.log(k));
        return parseFloat((bytes / Math.pow(k, i)).toFixed(1)) + ' ' + ['B', 'KB', 'MB'][i];
    }

    // --- AGGRESSIVE COMPRESSION ENGINE ---
    async function processActiveItem() {
        const item = queue[activeIndex];
        document.getElementById('activeFileName').innerText = item.file.name;
        viewBefore.src = item.originalUrl;
        document.getElementById('sizeBefore').innerText = formatBytes(item.file.size);
        
        miniLoader.classList.remove('d-none');

        try {
            const options = {
                maxSizeMB: 1,
                initialQuality: item.quality / 100,
                useWebWorker: true,
                maxIteration: 15
            };

            const blob = await imageCompression(item.file, options);
            item.compressedBlob = blob;
            item.compressedUrl = URL.createObjectURL(blob);

            viewAfter.src = item.compressedUrl;
            document.getElementById('sizeAfter').innerText = formatBytes(blob.size);
            
            const savings = Math.max(0, Math.round(((item.file.size - blob.size) / item.file.size) * 100));
            document.getElementById('savingsPct').innerText = `-${savings}%`;
            document.querySelectorAll('.q-savings')[activeIndex].innerText = `-${savings}%`;

        } catch (e) {
            console.error(e);
        } finally {
            miniLoader.classList.add('d-none');
        }
    }

    // --- HANDLE MULTI-FILE SELECTION ---
    fileInput.addEventListener('change', async (e) => {
        const files = Array.from(e.target.files);
        if (files.length === 0) return;

        uploadContainer.style.display = 'none';
        processingArea.style.display = 'block';

        files.forEach((file, i) => {
            const item = {
                file: file,
                originalUrl: URL.createObjectURL(file),
                quality: 70,
                compressedBlob: null,
                compressedUrl: null
            };
            queue.push(item);

            // Create UI Thumbnail
            const thumb = document.createElement('div');
            thumb.className = `q-thumb ${i === 0 ? 'active' : ''}`;
            thumb.innerHTML = `<img src="${item.originalUrl}"><span class="q-savings">...</span>`;
            thumb.onclick = () => {
                activeIndex = i;
                document.querySelectorAll('.q-thumb').forEach(t => t.classList.remove('active'));
                thumb.classList.add('active');
                qIn.value = queue[activeIndex].quality;
                qVal.innerText = qIn.value;
                processActiveItem();
            };
            queueContainer.appendChild(thumb);
        });

        processActiveItem();
    });

    // --- INTERACTIVE QUALITY OVERRIDE ---
    let debounce;
    qIn.oninput = (e) => {
        const val = e.target.value;
        qVal.innerText = val;
        queue[activeIndex].quality = parseInt(val);
        clearTimeout(debounce);
        debounce = setTimeout(processActiveItem, 500);
    };

    // --- BULK DOWNLOAD (ZIP) ---
    document.getElementById('btnDownloadAll').onclick = async () => {
        const zip = new JSZip();
        queue.forEach(item => {
            if (item.compressedBlob) {
                zip.file(`compressed-${item.file.name}`, item.compressedBlob);
            }
        });
        const content = await zip.generateAsync({ type: "blob" });
        const link = document.createElement('a');
        link.href = URL.createObjectURL(content);
        link.download = "zyntool-compressed.zip";
        link.click();
    };

    document.getElementById('btnDownloadActive').onclick = () => {
        const item = queue[activeIndex];
        if (!item.compressedBlob) return;
        const a = document.createElement('a');
        a.href = item.compressedUrl;
        a.download = `optimized-${item.file.name}`;
        a.click();
    };
</script>
